[tool.poetry]
name = "backend"
version = "0.1.0"
description = ""
authors = ["bokyum kim"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
aiohttp = "^3.11.11"
redis = "^5.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
